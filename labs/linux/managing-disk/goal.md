# Goal

Understand how Linux disk management works in practice.

Tasks:

- Identify disks and partitions using lsblk
- Create a new partition using fdisk
- Format partition with ext4 filesystem
- Mount partition manually
- Verify mount point
- Unmount partition safely
- Understand how /etc/fstab works
