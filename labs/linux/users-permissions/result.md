## Result

-Understood that UID identifies a user, and GID identifies a group
-/etc/passwd stores user records; /etc/shadow stores password hashes
-Practiced adding user to group with "usermod -aG" (append, not replace)
-Tested how 'chmod' changes permissions and 'chown' changes ownership
